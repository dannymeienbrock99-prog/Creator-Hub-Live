name: Android APK Build Windows

on:
  workflow_dispatch:
  push:
    branches: ["main"]

concurrency:
  group: creatorhub-android-windows-${{ github.ref }}
  cancel-in-progress: true

jobs:
  disabled:
    if: ${{ false }}
    runs-on: windows-latest
    steps:
      - run: echo "Disabled. Stable APK Builder no marketplace actions is active."
