name: Android Startup Smoke Test

on:
  workflow_dispatch:
  push:
    branches: ["main"]

concurrency:
  group: creatorhub-android-smoke-${{ github.ref }}
  cancel-in-progress: true

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled temporarily while GitHub marketplace actions are unavailable."
